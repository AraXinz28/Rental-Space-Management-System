# 📘 README — Setup สำหรับผู้ใช้ใหม่

โปรเจกต์: **Rental-Space-Management-System**
เทคโนโลยีหลัก: Java, JavaFX, Maven, Supabase

---

## ✅ 1. ดาวน์โหลดไฟล์ที่จำเป็น

ให้ดาวน์โหลดไฟล์จากโฟลเดอร์ **OOP setup**

* **OpenJDK 25 (JDK)** → ใช้รัน Java
* **openjfx-25_windows-x64_bin-sdk.zip** → ไลบรารี JavaFX
* **apache-maven-3.9.11-bin.zip** → Maven
* **SceneBuilder-25.0.0.msi** → UI Designer (ถ้าต้องการแก้ FXML)

---

## ✅ 2. ติดตั้ง / แตกไฟล์

### ✔️ 2.1 ติดตั้ง JDK

แตกไฟล์แล้ววางในตำแหน่ง เช่น:

```
C:\Program Files\OpenJDK\
```

### ✔️ 2.2 แตกไฟล์ JavaFX

```
C:\javafx-sdk-25\
```

### ✔️ 2.3 แตกไฟล์ Maven

```
C:\Program Files\apache-maven-3.9.11\
```

### ✔️ 2.4 ติดตั้ง SceneBuilder

ดับเบิลคลิกไฟล์ `.msi` และติดตั้งตามปกติ

---

## ✅ 3. ตั้งค่า PATH ใน Windows

### ตรวจสอบ Java

เปิด CMD แล้วพิมพ์:

```
java -version
```

ถ้าถูกต้องจะขึ้นเวอร์ชัน เช่น:

```
openjdk version "25..."
```

### ตั้งค่า PATH ของ Maven

1. เปิด **Environment Variables**
2. เพิ่ม PATH ตัวอย่าง:

```
C:\Program Files\apache-maven-3.9.11\bin
```

3. ทดสอบ Maven:

```
mvn -version
```

---

## ✅ 4. Clone โปรเจกต์จาก GitHub

เปิด CMD แล้วพิมพ์:

```
git clone https://github.com/AraXinz28/Rental-Space-Management-System.git
```

เข้าไปในโฟลเดอร์โปรเจกต์:

```
cd Rental-Space-Management-System
```

---

## ✅ 5. เปิดโปรเจกต์ใน VS Code

* เปิด VS Code
* เลือกโฟลเดอร์ `Rental-Space-Management-System`
* ติดตั้ง Extensions ที่จำเป็น:

  * Extension Pack for Java
  * Maven for Java
  * JavaFX Support (optional)

---

## ✅ 6. สร้างไฟล์ `.env`

สร้างไฟล์ชื่อ:

```
.env
```

แล้วใส่ค่า:

```
SUPABASE_URL=...
SUPABASE_KEY=...
```

(ใช้ค่าที่ทีมงานให้ไว้)

---

## ✅ 7. Build โปรเจกต์ด้วย Maven

เปิด Terminal ใน VS Code และรัน:

```
mvn clean install
```

คำสั่งนี้จะ compile และตรวจสอบ dependencies ทั้งหมด

---

## ✅ 8. รันโปรเจกต์

ถ้าโปรเจกต์ใช้ Maven Exec Plugin ให้รัน:

```
mvn exec:java
```

ถ้าถูกต้อง หน้าต่าง JavaFX จะเปิดขึ้น

---

# 🎉 เสร็จสิ้น!

โปรเจกต์พร้อมใช้งาน สามารถแก้ UI ด้วย SceneBuilder แก้โค้ด และเชื่อมต่อ Supabase ได้เรียบร้อย
